# @gocell/observability

This retained package owns only the RSS health foundation: health/system
requests, polling, stores, formatting, cards, and the landing health overview.

Hosted metrics, logs, traces, dashboards, and their `/observe` product surface
are intentionally absent. Their external systems are not owned by RSS Web.
