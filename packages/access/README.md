# @gocell/access

Retained migration package for login/session state, local identity and role
administration, policies, and PDP decisions. Public entries are declared in
`package.json#exports`; HTTP calls use `@gocell/request` and current data shapes
come from the frozen `@gocell/contracts` bridge.

First-run provisioning and placeholder access pages are intentionally absent.
