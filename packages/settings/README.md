# @rss/settings

Framework-neutral strict adapters for selected active RSS Settings contracts. The package owns no UI,
storage, tenant/header authority, catalog/history projection, retry loop, or contract runtime parser.
