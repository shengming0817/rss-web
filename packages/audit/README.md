# @gocell/audit

Retained migration package for the audit log API, store, filters, and view.
It consumes the frozen migration type bridge and sends HTTP requests only
through `@gocell/request`. Public entries are declared in `package.json#exports`.
